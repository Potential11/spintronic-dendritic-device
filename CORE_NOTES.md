# SOT 单次反转版本

The formulation considered here is restricted to a single-reversal protocol.

## 适用协议

从 INITIAL_X（默认 0 mA）开始，输入电流只能单调增加后单调减小，或单调减小后单调增加；也允许没有反转。这里“反转”是电流数值的扫描方向变化，不是电流过零。相同数值不计为反转；不设噪声容差。

- 允许：0 → +22 → −22。
- 允许：0 → −22 → +22。
- 拒绝：0 → +22 → −22 → 0，最后一步是第二次反转。
- 拒绝：0 → +25 → +20 → +22，最后一步是第二次反转。
- 每次独立实验先调用 reset()。它同时恢复磁化初态和协议计数；不能用于保留磁化状态的多轮回扫。

第二次反转抛出 ValueError，违规点不改变模型状态。跨 step()/run() 调用累计检查；批量 run() 中在错误之前已接受的点保留，不做整批回滚。NaN/Inf 也会报错。电压耦合模式检查的是换算后的有效 SOT 电流方向。

## 模型规则

正向目标始终为 IC_BAO（默认 +29 mA），不再根据负向峰值缩短。正电流超过 +17 mA 且增大时，由分支起始 M 沿 S 形曲线上升至 0.65；下降时保持。

反向目标仍由正向峰值决定：有超过 +17 mA 的正峰值时，neg_target=max(-pos_peak,-29)，否则为 −29 mA。负电流低于 −17 mA 时，沿对应分支计算至 M=0.30。

本次没有修改负分支本身的插值或回扫计算。协议检查只拒绝第二次方向反转，不会将原算法变成任意历史都成立的磁滞模型；例如先向负扫再回升的第一次回扫仍按原负分支公式计算。模型没有显式时间、脉宽、温度或微磁动力学方程。

另一个历史实现 requested_memory_roundtrip 已从此包移除，以免绕过协议检查；原始研究目录中的文件没有修改。triangular_sweep 仅生成数组，本身不执行模型；从默认初始电流 0 跳到负端点再进行完整负→正→负扫描，会产生两次方向反转并被拒绝。当前 demo 将 INITIAL_X 显式设为 −35 mA、INITIAL_M 设为 0.30，再执行 −35→+35→−35 mA，因此只反转一次。

## 电阻网络与参数

仅对导通支路：V_common=Σ(V_i/R_i)/Σ(1/R_i)，I_i=(V_i−V_common)/R_i，x_mA=−1000×I_output。固定导通状态下电阻网络是线性的，非线性来自 SOT 响应。

默认电阻 304、500、751、844、980 Ω；耦合函数只取前四个，开启端口 1、2、4。端口 1 为 −bias_abs_v，默认偏置幅度 8 V；端口 2/4 为输入 A/B。B=0 V 是接地，不是断路。第五端口不能直接用于该耦合函数。底层列表应等长、电阻为正，至少一支路导通；这些输入约束仍由调用者保证。

默认磁化初态 0.30，饱和端点 0.30/0.65，阈值 ±17 mA，完整分支端点 ±29 mA，k=2.5，左右速率均为 0.8，曲线 tanh，更新模式 branch_fixed。

stateless_sot_response 和 run_plot2_condition 每点新建模型，不能用来检测跨点历史。SOTSwitchingModel 与 NetworkSOTModel 保留历史并检查协议。run() 的 delta_M 是相对 INITIAL_M 的变化，而非逐步增量。

simulate_one_bias 的 x_ref=ΔM(A,0)+ΔM(0,0)，零负输入曲线直接设为 x_ref；其余为真实条件响应。改变参数后基线可能不为零，需区分参考曲线和实际输出。示例 CSV 使用直接条件计算值。
